name: banorte_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter
  local_auth: ^2.1.5
  screen_protector: ^1.0.1
  image_picker: ^1.1.2
  cupertino_icons: ^1.0.8
  pdf: ^3.6.0
  printing: ^5.7.0
  path_provider: ^2.0.11
  permission_handler: ^11.4.0  # Añadido para manejar permisos



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
